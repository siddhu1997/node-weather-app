<!DOCTYPE html>

<html>
    <head>
        <title>Error!</title>
        <link rel="icon" href="images/Weather.png"/>
        <link rel="stylesheet" href="css/style.css"/>
    </head>
    <body>
        <div class="main-content">
            {{>header}}
            <p>{{errorMessage}}</p>
        </div>
        {{>footer}}
    </body>
</html>